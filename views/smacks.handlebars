<section id="gigs" class="container">
    <h1>All Smacks</h1>

    {{#each smacks}}
      <div class="gig">
      <h3>{{team}}</h3>
      <p>{{smack}}</p>
    {{else}}
      <h2>No smacks available</h2>
    {{/each}}
    
  </section>